RSpec.describe Lorentz::Contract::Originate do
  it "has a version number" do
    expect(Lorentz::Contract::Originate::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
